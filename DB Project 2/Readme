Instruction to run the Project:
1. Unzip Project2
2. In terminal go to directory path pf newDB. 
3. Go in directory "newDB" on terminal
4. write command "sbt"
5. On getting sbt command prompt, write command "compile".
This should compile the all the modules.
6. Optionally, run command "test". this will compile test Junits.
7. execute command "run". It will ask you which class to run?
8. Select class "MovieDB" by entering corresponding number 
9. Run for TreeMap/LinHashMap/ BpTreeMap
   To test above three cases Go to src>main>javaTable.java
10. In this java program go to Constructors part.
    There are two Table Constructor. In this constructor uncomment whichever part you want to run and comment other two. Do this in both constructors.
    (Line 111,112,113 & Line 140,141,142 )
    Note : Don't forget to compile after you comment/uncomment things. An run from the same class "MovieDB".
   
